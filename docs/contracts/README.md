# Contracts

Contracts are central agreements shared by every workstream. Workstreams must
read the relevant contracts before producing outputs and must propose changes
instead of editing contracts directly.

The Orchestrator owns final contract updates.
