# Issue Tracker

This project defaults to local markdown issues under `docs/issues/`.

Use one file per vertical slice:

```text
docs/issues/NN-short-slug.md
```

Each issue should include:

- Title
- Type: `AFK` or `HITL`
- Blocked by
- What to build
- Acceptance criteria
- Out of scope

