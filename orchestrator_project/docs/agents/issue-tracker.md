# Issue Tracker

This scaffold defaults to local markdown issues under `docs/issues/`.

When publishing a PRD, write it under `docs/prd/`.

When publishing implementation issues, write them under `docs/issues/`.

