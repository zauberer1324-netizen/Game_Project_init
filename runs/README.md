# Runs

This folder stores execution logs, generated prompts, merge previews, and other
runtime artifacts when a workflow chooses to persist them.

A fresh clone may contain only `.gitkeep`; that is normal. Most dry-runs print
to the terminal unless a script is called with a write option.
